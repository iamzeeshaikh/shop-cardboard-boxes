---
import BaseLayout from '../layouts/BaseLayout.astro';
import { renderHome } from '../lib/seo/render-home';

const snapshot = renderHome();
---

<BaseLayout snapshot={snapshot}>
  <Fragment set:html={snapshot.contentHtml} />
</BaseLayout>
